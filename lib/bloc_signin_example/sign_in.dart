import 'package:flutter/material.dart';

class SignInBlocExample extends StatelessWidget {
  const SignInBlocExample({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Sign In With Bloc'),
      ),
    );
  }
}